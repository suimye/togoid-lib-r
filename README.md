# Figures for the enrichment + UMAP use case (R)

Figures generated by `inst/examples/scRNAseq_enrichment` on the 10x Genomics
public PBMC dataset (3,733 cells after QC, 15 Seurat clusters).

This branch only holds images, so the feature branch stays free of binaries.
